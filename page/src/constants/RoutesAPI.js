const baseAPI = "http://localhost:3001"

const RouteAPI = {
    Posts: `${baseAPI}/posts`,
    Auth: `${baseAPI}/auth`,
    Users: `${baseAPI}/users`,
    Images: `${baseAPI}/images`
}

export { RouteAPI }